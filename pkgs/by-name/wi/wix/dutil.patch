diff --git a/src/libs/dutil/WixToolset.DUtil/apuputil.cpp b/src/libs/dutil/WixToolset.DUtil/apuputil.cpp
index eb96d515..1acdd291 100644
--- a/src/libs/dutil/WixToolset.DUtil/apuputil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/apuputil.cpp
@@ -147,7 +147,7 @@ HRESULT DAPI ApupFilterChain(
     HRESULT hr = S_OK;
     APPLICATION_UPDATE_CHAIN* pNewChain = NULL;
     APPLICATION_UPDATE_ENTRY* prgEntries = NULL;
-    DWORD cEntries = NULL;
+    DWORD cEntries = 0;
 
     pNewChain = static_cast<APPLICATION_UPDATE_CHAIN*>(MemAlloc(sizeof(APPLICATION_UPDATE_CHAIN), TRUE));
     ApupExitOnNull(pNewChain, hr, E_OUTOFMEMORY, "Failed to allocate filtered chain.");
diff --git a/src/libs/dutil/WixToolset.DUtil/atomutil.cpp b/src/libs/dutil/WixToolset.DUtil/atomutil.cpp
index d6fd3890..2bb8f4ea 100644
--- a/src/libs/dutil/WixToolset.DUtil/atomutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/atomutil.cpp
@@ -927,6 +927,7 @@ static HRESULT ParseAtomUnknownElement(
     IXMLDOMNamedNodeMap* pixnnmAttributes = NULL;
     IXMLDOMNode* pixnAttribute = NULL;
     ATOM_UNKNOWN_ELEMENT* pNewUnknownElement;
+    ATOM_UNKNOWN_ELEMENT** ppTail = ppUnknownElement;
 
     pNewUnknownElement = (ATOM_UNKNOWN_ELEMENT*)MemAlloc(sizeof(ATOM_UNKNOWN_ELEMENT), TRUE);
     AtomExitOnNull(pNewUnknownElement, hr, E_OUTOFMEMORY, "Failed to allocate unknown element.");
@@ -972,7 +973,6 @@ static HRESULT ParseAtomUnknownElement(
     }
     AtomExitOnFailure(hr, "Failed to enumerate all attributes on ATOM unknown element.");
 
-    ATOM_UNKNOWN_ELEMENT** ppTail = ppUnknownElement;
     while (*ppTail)
     {
         ppTail = &(*ppTail)->pNext;
@@ -1010,6 +1010,7 @@ static HRESULT ParseAtomUnknownAttribute(
     BSTR bstrNodeName = NULL;
     BSTR bstrNodeValue = NULL;
     ATOM_UNKNOWN_ATTRIBUTE* pNewUnknownAttribute;
+    ATOM_UNKNOWN_ATTRIBUTE** ppTail = ppUnknownAttribute;
 
     pNewUnknownAttribute = (ATOM_UNKNOWN_ATTRIBUTE*)MemAlloc(sizeof(ATOM_UNKNOWN_ATTRIBUTE), TRUE);
     AtomExitOnNull(pNewUnknownAttribute, hr, E_OUTOFMEMORY, "Failed to allocate unknown attribute.");
@@ -1038,7 +1039,6 @@ static HRESULT ParseAtomUnknownAttribute(
     hr = StrAllocString(&pNewUnknownAttribute->wzValue, bstrNodeValue, 0);
     AtomExitOnFailure(hr, "Failed to allocate ATOM unknown attribute value.");
 
-    ATOM_UNKNOWN_ATTRIBUTE** ppTail = ppUnknownAttribute;
     while (*ppTail)
     {
         ppTail = &(*ppTail)->pNext;
diff --git a/src/libs/dutil/WixToolset.DUtil/cabcutil.cpp b/src/libs/dutil/WixToolset.DUtil/cabcutil.cpp
index 294669af..e8401045 100644
--- a/src/libs/dutil/WixToolset.DUtil/cabcutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/cabcutil.cpp
@@ -200,6 +200,8 @@ extern "C" HRESULT DAPI CabCBegin(
 
     C_ASSERT(sizeof(MSIFILEHASHINFO) == 20);
 
+    size_t cbFileAllocSize = 0;
+
     LPWSTR pwzPathBuffer = NULL;
     if (wzCabDir)
     {
@@ -221,7 +223,7 @@ extern "C" HRESULT DAPI CabCBegin(
 
     pcd->fileSplitCabNamesCallback = NULL;
 
-    if (NULL == dwMaxSize)
+    if (0 == dwMaxSize)
     {
         pcd->ccab.cb = CAB_MAX_SIZE;
         pcd->fCabinetSplittingEnabled = FALSE; // If no max cab size is supplied, cabinet splitting is not desired
@@ -302,7 +304,6 @@ extern "C" HRESULT DAPI CabCBegin(
     }
 
     pcd->cMaxFilePaths = dwMaxFiles;
-    size_t cbFileAllocSize = 0;
 
     hr = ::SizeTMult(pcd->cMaxFilePaths, sizeof(CABC_FILE), &(cbFileAllocSize));
     CabcExitOnFailure(hr, "Maximum allocation exceeded on initialization.");
@@ -826,6 +827,7 @@ static HRESULT AddNonDuplicateFile(
 {
     HRESULT hr = S_OK;
     LPVOID pv = NULL;
+    CABC_FILE *pcf = NULL;
 
     // Ensure there is enough memory to store this file index.
     if (pcd->cFilePaths == pcd->cMaxFilePaths)
@@ -847,7 +849,7 @@ static HRESULT AddNonDuplicateFile(
 
     // Store the file index information.
     // TODO: add this to a sorted list so we can do a binary search later.
-    CABC_FILE *pcf = pcd->prgFiles + pcd->cFilePaths;
+    pcf = pcd->prgFiles + pcd->cFilePaths;
     pcf->dwCabFileIndex = dwCabFileIndex;
     pcf->llFileSize = llFileSize;
 
@@ -1366,6 +1368,7 @@ static __callback BOOL DIAMONDAPI CabCGetNextCabinet(
     HRESULT hr = S_OK;
     LPWSTR pwzFileToken = NULL;
     WCHAR wzNewCabName[MAX_PATH] = L"";
+    const int nAlphabets = 26;
 
     if (pccab->iCab == 1)
     {
@@ -1380,7 +1383,6 @@ static __callback BOOL DIAMONDAPI CabCGetNextCabinet(
         CabcExitOnFailure(hr, "Failed to remove extension to create next Cabinet File Name");
     }
 
-    const int nAlphabets = 26; // Number of Alphabets from a to z
     if (pccab->iCab <= nAlphabets)
     {
         // Construct next cab names like cab1a.cab, cab1b.cab, cab1c.cab, ........
diff --git a/src/libs/dutil/WixToolset.DUtil/cabutil.cpp b/src/libs/dutil/WixToolset.DUtil/cabutil.cpp
index 57463e1a..eac78b60 100644
--- a/src/libs/dutil/WixToolset.DUtil/cabutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/cabutil.cpp
@@ -574,7 +574,7 @@ static __callback INT_PTR DIAMONDAPI CabExtractCallback(__in FDINOTIFICATIONTYPE
         hr = StrAllocStringAnsi(&pwz, sz, 0, CP_ACP);
         CabExitOnFailure(hr, "failed to convert cabinet file id to unicode: %hs", sz);
 
-        if (NULL != pFDINotify->hf)  // just close the file
+        if (0 != pFDINotify->hf)  // just close the file
         {
             ::CloseHandle(reinterpret_cast<HANDLE>(pFDINotify->hf));
         }
diff --git a/src/libs/dutil/WixToolset.DUtil/certutil.cpp b/src/libs/dutil/WixToolset.DUtil/certutil.cpp
index 69897b9e..0b1dec77 100644
--- a/src/libs/dutil/WixToolset.DUtil/certutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/certutil.cpp
@@ -248,7 +248,7 @@ extern "C" BOOL DAPI CertHasPrivateKey(
     __in PCCERT_CONTEXT pCertContext,
     __out_opt DWORD* pdwKeySpec)
 {
-    HCRYPTPROV_OR_NCRYPT_KEY_HANDLE hPrivateKey = NULL;
+    HCRYPTPROV_OR_NCRYPT_KEY_HANDLE hPrivateKey = 0;
     DWORD dwKeySpec = 0;
     // set CRYPT_ACQUIRE_CACHE_FLAG so that we don't have to release the private key handle
     BOOL fResult = ::CryptAcquireCertificatePrivateKey(
@@ -278,7 +278,7 @@ extern "C" HRESULT DAPI CertInstallSingleCertificate(
 
     DWORD dwKeySpec = 0;
 
-    HCRYPTPROV hCsp = NULL;
+    HCRYPTPROV hCsp = 0;
     LPWSTR pwszTmpContainer = NULL;
     LPWSTR pwszProviderName = NULL;
     DWORD dwProviderType = 0;
diff --git a/src/libs/dutil/WixToolset.DUtil/conutil.cpp b/src/libs/dutil/WixToolset.DUtil/conutil.cpp
index 059af0e0..c5a064b5 100644
--- a/src/libs/dutil/WixToolset.DUtil/conutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/conutil.cpp
@@ -379,8 +379,6 @@ extern "C" HRESULT DAPI ConsoleReadNonBlockingW(
 
     LPSTR psz = NULL;
 
-    ConExitOnNull(ppwzBuffer, hr, E_INVALIDARG, "Failed to read from console because buffer was not provided");
-
     DWORD dwRead;
     DWORD dwNumInput;
 
@@ -393,6 +391,8 @@ extern "C" HRESULT DAPI ConsoleReadNonBlockingW(
     INPUT_RECORD ir;
     WCHAR chIn;
 
+    ConExitOnNull(ppwzBuffer, hr, E_INVALIDARG, "Failed to read from console because buffer was not provided");
+
     *ppwzBuffer = NULL;
     *pcchSize = 0;
 
diff --git a/src/libs/dutil/WixToolset.DUtil/cryputil.cpp b/src/libs/dutil/WixToolset.DUtil/cryputil.cpp
index 24bb83f1..5e108848 100644
--- a/src/libs/dutil/WixToolset.DUtil/cryputil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/cryputil.cpp
@@ -214,8 +214,8 @@ extern "C" HRESULT DAPI CrypHashFileHandle(
     )
 {
     HRESULT hr = S_OK;
-    HCRYPTPROV hProv = NULL;
-    HCRYPTHASH hHash = NULL;
+    HCRYPTPROV hProv = 0;
+    HCRYPTHASH hHash = 0;
     DWORD cbRead = 0;
     BYTE rgbBuffer[4096] = { };
     const LARGE_INTEGER liZero = { };
@@ -289,8 +289,8 @@ HRESULT DAPI CrypHashBuffer(
     )
 {
     HRESULT hr = S_OK;
-    HCRYPTPROV hProv = NULL;
-    HCRYPTHASH hHash = NULL;
+    HCRYPTPROV hProv = 0;
+    HCRYPTHASH hHash = 0;
     DWORD cbDataHashed = 0;
     SIZE_T cbTotal = 0;
     SIZE_T cbRemaining = 0;
@@ -310,7 +310,7 @@ HRESULT DAPI CrypHashBuffer(
     do
     {
         cbRemaining = cbBuffer - cbTotal;
-        cbDataHashed = (DWORD)min(DWORD_MAX, cbRemaining);
+        cbDataHashed = (DWORD)min((SIZE_T)DWORD_MAX, cbRemaining);
         if (!::CryptHashData(hHash, pbBuffer + cbTotal, cbDataHashed, 0))
         {
             CrypExitWithLastError(hr, "Failed to hash data.");
diff --git a/src/libs/dutil/WixToolset.DUtil/dictutil.cpp b/src/libs/dutil/WixToolset.DUtil/dictutil.cpp
index 09b150df..88c35cf6 100644
--- a/src/libs/dutil/WixToolset.DUtil/dictutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/dictutil.cpp
@@ -339,11 +339,12 @@ extern "C" HRESULT DAPI DictGetValue(
     )
 {
     HRESULT hr = S_OK;
+    const STRINGDICT_STRUCT *psd = NULL;
 
     DictExitOnNull(sdHandle, hr, E_INVALIDARG, "Handle not specified while searching dict");
     DictExitOnNull(pszString, hr, E_INVALIDARG, "String not specified while searching dict");
 
-    const STRINGDICT_STRUCT *psd = static_cast<const STRINGDICT_STRUCT *>(sdHandle);
+    psd = static_cast<const STRINGDICT_STRUCT *>(sdHandle);
 
     if (DICT_EMBEDDED_KEY != psd->dtType)
     {
@@ -368,11 +369,12 @@ extern "C" HRESULT DAPI DictKeyExists(
     )
 {
     HRESULT hr = S_OK;
+    const STRINGDICT_STRUCT *psd = NULL;
 
     DictExitOnNull(sdHandle, hr, E_INVALIDARG, "Handle not specified while searching dict");
     DictExitOnNull(pszString, hr, E_INVALIDARG, "String not specified while searching dict");
 
-    const STRINGDICT_STRUCT *psd = static_cast<const STRINGDICT_STRUCT *>(sdHandle);
+    psd = static_cast<const STRINGDICT_STRUCT *>(sdHandle);
 
     // This works with either type of dictionary
     hr = GetValue(psd, pszString, NULL);
@@ -417,6 +419,7 @@ static HRESULT CreateDict(
     )
 {
     HRESULT hr = S_OK;
+    STRINGDICT_STRUCT *psd = NULL;
 
     DictExitOnNull(psdHandle, hr, E_INVALIDARG, "Handle not specified while creating dict.");
 
@@ -424,7 +427,7 @@ static HRESULT CreateDict(
     *psdHandle = static_cast<STRINGDICT_HANDLE>(MemAlloc(sizeof(STRINGDICT_STRUCT), TRUE));
     DictExitOnNull(*psdHandle, hr, E_OUTOFMEMORY, "Failed to allocate dictionary object.");
 
-    STRINGDICT_STRUCT* psd = static_cast<STRINGDICT_STRUCT*>(*psdHandle);
+    psd = static_cast<STRINGDICT_STRUCT*>(*psdHandle);
 
     // Fill out the new handle's values
     psd->dtType = dtType;
@@ -524,6 +527,7 @@ static HRESULT GetValue(
     DWORD dwOriginalIndexCandidate = 0;
     void *pvCandidateValue = NULL;
     DWORD dwIndex = 0;
+    DWORD dwIndexCandidate = 0;
 
     DictExitOnNull(psd, hr, E_INVALIDARG, "Handle not specified while searching dict");
     DictExitOnNull(pszString, hr, E_INVALIDARG, "String not specified while searching dict");
@@ -537,7 +541,7 @@ static HRESULT GetValue(
     hr = StringHash(psd, MAX_BUCKET_SIZES[psd->dwBucketSizeIndex], pszString, &dwOriginalIndexCandidate);
     DictExitOnFailure(hr, "Failed to hash the string.");
 
-    DWORD dwIndexCandidate = dwOriginalIndexCandidate;
+    dwIndexCandidate = dwOriginalIndexCandidate;
 
     pvCandidateValue = TranslateOffsetToValue(psd, psd->ppvBuckets[dwIndexCandidate]);
 
@@ -582,11 +586,12 @@ static HRESULT GetInsertIndex(
 {
     HRESULT hr = S_OK;
     DWORD dwOriginalIndexCandidate = 0;
+    DWORD dwIndexCandidate = 0;
 
     hr = StringHash(psd, dwBucketCount, pszString, &dwOriginalIndexCandidate);
     DictExitOnFailure(hr, "Failed to hash the string.");
 
-    DWORD dwIndexCandidate = dwOriginalIndexCandidate;
+    dwIndexCandidate = dwOriginalIndexCandidate;
 
     // If we collide, keep iterating forward from our intended position, even wrapping around to zero, until we find an empty bucket
 #pragma prefast(push)
@@ -625,6 +630,7 @@ static HRESULT GetIndex(
 {
     HRESULT hr = S_OK;
     DWORD dwOriginalIndexCandidate = 0;
+    DWORD dwIndexCandidate = 0;
 
     if (psd->dwBucketSizeIndex >= countof(MAX_BUCKET_SIZES))
     {
@@ -635,7 +641,7 @@ static HRESULT GetIndex(
     hr = StringHash(psd, MAX_BUCKET_SIZES[psd->dwBucketSizeIndex], pszString, &dwOriginalIndexCandidate);
     DictExitOnFailure(hr, "Failed to hash the string.");
 
-    DWORD dwIndexCandidate = dwOriginalIndexCandidate;
+    dwIndexCandidate = dwOriginalIndexCandidate;
 
     while (!IsMatchExact(psd, dwIndexCandidate, pszString))
     {
diff --git a/src/libs/dutil/WixToolset.DUtil/inc/cabutil.h b/src/libs/dutil/WixToolset.DUtil/inc/cabutil.h
index 0bedba80..93ed7728 100644
--- a/src/libs/dutil/WixToolset.DUtil/inc/cabutil.h
+++ b/src/libs/dutil/WixToolset.DUtil/inc/cabutil.h
@@ -3,7 +3,7 @@
 
 
 #include <fdi.h>
-#include <sys\stat.h>
+#include <sys/stat.h>
 
 #ifdef __cplusplus
 extern "C" {
diff --git a/src/libs/dutil/WixToolset.DUtil/inc/dutil.h b/src/libs/dutil/WixToolset.DUtil/inc/dutil.h
index 7d8c7d81..4885ee6b 100644
--- a/src/libs/dutil/WixToolset.DUtil/inc/dutil.h
+++ b/src/libs/dutil/WixToolset.DUtil/inc/dutil.h
@@ -120,7 +120,7 @@ void DAPI Dutil_RootFailure(__in_z LPCSTR szFile, __in int iLine, __in HRESULT h
 
 #define ExitFunctionWithLastError(x) { x = HRESULT_FROM_WIN32(::GetLastError()); goto LExit; }
 
-#define ExitTraceSource(d, x, s, ...) { TraceError(x, s, __VA_ARGS__); (void)Dutil_TraceErrorSource(__FILE__, __LINE__, REPORT_ERROR, d, x, s, __VA_ARGS__); }
+#define ExitTraceSource(d, x, s, ...) { TraceError(x, s __VA_OPT__(,) __VA_ARGS__); (void)Dutil_TraceErrorSource(__FILE__, __LINE__, REPORT_ERROR, d, x, s __VA_OPT__(,) __VA_ARGS__); }
 #define ExitTraceDebugSource(d, x, s, ...) { TraceErrorDebug(x, s, __VA_ARGS__); (void)Dutil_TraceErrorSource(__FILE__, __LINE__, REPORT_DEBUG, d, x, s, __VA_ARGS__); }
 
 #define ExitOnLastErrorSource(d, x, s, ...) { DWORD Dutil_er = ::GetLastError(); x = HRESULT_FROM_WIN32(Dutil_er); if (FAILED(x)) { Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceSource(d, x, s, __VA_ARGS__); goto LExit; } }
@@ -130,9 +130,9 @@ void DAPI Dutil_RootFailure(__in_z LPCSTR szFile, __in int iLine, __in HRESULT h
 #define ExitOnRootFailureSource(d, x, s, ...) if (FAILED(x)) { Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceSource(d, x, s, __VA_ARGS__);  goto LExit; }
 #define ExitWithRootFailureSource(d, x, e, s, ...) { x = FAILED(e) ? e : E_FAIL; Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceSource(d, x, s, __VA_ARGS__); goto LExit; }
 #define ExitOnFailureDebugTraceSource(d, x, s, ...) if (FAILED(x)) { ExitTraceDebugSource(d, x, s, __VA_ARGS__);  goto LExit; }
-#define ExitOnNullSource(d, p, x, e, s, ...) if (NULL == p) { x = e; Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceSource(d, x, s, __VA_ARGS__);  goto LExit; }
-#define ExitOnNullWithLastErrorSource(d, p, x, s, ...) if (NULL == p) { DWORD Dutil_er = ::GetLastError(); x = HRESULT_FROM_WIN32(Dutil_er); if (!FAILED(x)) { x = E_FAIL; } Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceSource(d, x, s, __VA_ARGS__); goto LExit; }
-#define ExitOnNullDebugTraceSource(d, p, x, e, s, ...) if (NULL == p) { x = e; Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceDebugSource(d, x, s, __VA_ARGS__);  goto LExit; }
+#define ExitOnNullSource(d, p, x, e, s, ...) if (0 == p) { x = e; Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceSource(d, x, s, __VA_ARGS__);  goto LExit; }
+#define ExitOnNullWithLastErrorSource(d, p, x, s, ...) if (0 == p) { DWORD Dutil_er = ::GetLastError(); x = HRESULT_FROM_WIN32(Dutil_er); if (!FAILED(x)) { x = E_FAIL; } Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceSource(d, x, s, __VA_ARGS__); goto LExit; }
+#define ExitOnNullDebugTraceSource(d, p, x, e, s, ...) if (0 == p) { x = e; Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceDebugSource(d, x, s, __VA_ARGS__);  goto LExit; }
 #define ExitOnInvalidHandleWithLastErrorSource(d, p, x, s, ...) if (INVALID_HANDLE_VALUE == p) { DWORD Dutil_er = ::GetLastError(); x = HRESULT_FROM_WIN32(Dutil_er); if (!FAILED(x)) { x = E_FAIL; } Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceSource(d, x, s, __VA_ARGS__); goto LExit; }
 #define ExitOnWin32ErrorSource(d, e, x, s, ...) if (ERROR_SUCCESS != e) { x = HRESULT_FROM_WIN32(e); if (!FAILED(x)) { x = E_FAIL; } Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceSource(d, x, s, __VA_ARGS__); goto LExit; }
 #define ExitOnOptionalXmlQueryFailureSource(d, x, b, s, ...) { { if (S_FALSE == x || E_NOTFOUND == x) { b = FALSE; x = S_OK; } else { b = SUCCEEDED(x); } }; ExitOnRootFailureSource(d, x, s, __VA_ARGS__); }
diff --git a/src/libs/dutil/WixToolset.DUtil/inc/monutil.h b/src/libs/dutil/WixToolset.DUtil/inc/monutil.h
index f644e205..1deca578 100644
--- a/src/libs/dutil/WixToolset.DUtil/inc/monutil.h
+++ b/src/libs/dutil/WixToolset.DUtil/inc/monutil.h
@@ -12,8 +12,7 @@ extern "C" {
 typedef void* MON_HANDLE;
 typedef const void* C_MON_HANDLE;
 
-// Defined in regutil.h
-enum REG_KEY_BITNESS;
+#include "regutil.h"
 
 extern const int MON_HANDLE_BYTES;
 
diff --git a/src/libs/dutil/WixToolset.DUtil/inc/wiutil.h b/src/libs/dutil/WixToolset.DUtil/inc/wiutil.h
index 71f40e8a..f00a6617 100644
--- a/src/libs/dutil/WixToolset.DUtil/inc/wiutil.h
+++ b/src/libs/dutil/WixToolset.DUtil/inc/wiutil.h
@@ -1,6 +1,7 @@
 #pragma once
 // Copyright (c) .NET Foundation and contributors. All rights reserved. Licensed under the Microsoft Reciprocal License. See LICENSE.TXT file in the project root for full license information.
 
+#include <msi.h>
 
 #ifdef __cplusplus
 extern "C" {
diff --git a/src/libs/dutil/WixToolset.DUtil/precomp.h b/src/libs/dutil/WixToolset.DUtil/precomp.h
index 81dc7992..e7b275f6 100644
--- a/src/libs/dutil/WixToolset.DUtil/precomp.h
+++ b/src/libs/dutil/WixToolset.DUtil/precomp.h
@@ -3,16 +3,58 @@
 
 
 #ifndef _WIN32_WINNT
-#define _WIN32_WINNT 0x0500
+#define _WIN32_WINNT 0x0600
 #endif
 
 #ifndef _WIN32_MSI
-#define _WIN32_MSI 200
+#define _WIN32_MSI 500
 #endif
 
-#define JET_VERSION 0x0501
+#define JET_VERSION 0x0600  // work around a bug in MinGW esent.h
 
-#include <WinSock2.h>
+#include <sal.h>
+#define __in
+#define __in_bcount_opt(b)
+#define __in_z
+#define __in_z_opt
+#define __in_ecount_opt(b)
+#define __in_xcount(b)
+#define __out
+#define __out_bcount_part_opt(x,y)
+#define __out_data_source(x)
+#define __out_ecount_opt(b)
+#define __out_range(x,y)
+#define __out_z
+#define __out_z_opt
+#define __inout_opt
+#define __inout_z
+#define __inout_z_opt
+#define __deref_opt_out_z
+#define __deref_opt_out_z_opt
+#define __deref_out_bcount(b)
+#define __deref_out_bcount_full(b)
+#define __deref_out_bcount_opt(b)
+#define __deref_out_bound
+#define __deref_out_ecount_opt(b)
+#define __deref_out_ecount_part(x,y)
+#define __deref_out_ecount_z(x)
+#define __deref_out_z
+#define __deref_out_z_opt
+#define __deref_inout
+#define __deref_inout_bcount(b)
+#define __deref_inout_bcount_full(b)
+#define __deref_inout_ecount(b)
+#define __deref_inout_ecount_opt(x)
+#define __deref_inout_ecount_part(x,y)
+#define __deref_inout_z
+#define __deref_inout_z_opt
+#define __format_string
+#define __nullnullterminated
+#define __reserved
+#define __success(e)
+#define __callback
+
+#include <winsock2.h>
 #include <windows.h>
 #include <windowsx.h>
 #include <intsafe.h>
@@ -24,22 +66,21 @@
 #include <shlobj.h>
 #include <shlwapi.h>
 #include <gdiplus.h>
-#include <Tlhelp32.h>
+#include <tlhelp32.h>
 #include <lm.h>
-#include <Iads.h>
+#include <iads.h>
 #include <activeds.h>
 #include <richedit.h>
 #include <stddef.h>
 #include <esent.h>
-#include <ahadmin.h>
-#include <SRRestorePtAPI.h>
+#include <srrestoreptapi.h>
 #include <userenv.h>
-#include <WinIoCtl.h>
+#include <winioctl.h>
 #include <wtsapi32.h>
 #include <wuapi.h>
 #include <commctrl.h>
 #include <dbt.h>
-#include <ShellScalingApi.h>
+#include <shellscalingapi.h>
 
 #include "dutilsources.h"
 #include "dutil.h"
@@ -102,3 +143,10 @@
 #include <msxml2.h>   // This file is needed to include xmlutil.h
 #include "xmlutil.h"
 
+#ifndef DWORD_MAX
+#define DWORD_MAX __MSABI_LONG(0xffffffffu)
+#endif
+#ifndef STRSAFE_MAX_LENGTH
+#define STRSAFE_MAX_LENGTH  (STRSAFE_MAX_CCH - 1)
+#endif
+#define min std::min

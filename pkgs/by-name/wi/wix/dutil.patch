diff --git a/src/libs/dutil/WixToolset.DUtil/apuputil.cpp b/src/libs/dutil/WixToolset.DUtil/apuputil.cpp
index eb96d515..1acdd291 100644
--- a/src/libs/dutil/WixToolset.DUtil/apuputil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/apuputil.cpp
@@ -147,7 +147,7 @@ HRESULT DAPI ApupFilterChain(
     HRESULT hr = S_OK;
     APPLICATION_UPDATE_CHAIN* pNewChain = NULL;
     APPLICATION_UPDATE_ENTRY* prgEntries = NULL;
-    DWORD cEntries = NULL;
+    DWORD cEntries = 0;
 
     pNewChain = static_cast<APPLICATION_UPDATE_CHAIN*>(MemAlloc(sizeof(APPLICATION_UPDATE_CHAIN), TRUE));
     ApupExitOnNull(pNewChain, hr, E_OUTOFMEMORY, "Failed to allocate filtered chain.");
diff --git a/src/libs/dutil/WixToolset.DUtil/atomutil.cpp b/src/libs/dutil/WixToolset.DUtil/atomutil.cpp
index d6fd3890..2bb8f4ea 100644
--- a/src/libs/dutil/WixToolset.DUtil/atomutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/atomutil.cpp
@@ -927,6 +927,7 @@ static HRESULT ParseAtomUnknownElement(
     IXMLDOMNamedNodeMap* pixnnmAttributes = NULL;
     IXMLDOMNode* pixnAttribute = NULL;
     ATOM_UNKNOWN_ELEMENT* pNewUnknownElement;
+    ATOM_UNKNOWN_ELEMENT** ppTail = ppUnknownElement;
 
     pNewUnknownElement = (ATOM_UNKNOWN_ELEMENT*)MemAlloc(sizeof(ATOM_UNKNOWN_ELEMENT), TRUE);
     AtomExitOnNull(pNewUnknownElement, hr, E_OUTOFMEMORY, "Failed to allocate unknown element.");
@@ -972,7 +973,6 @@ static HRESULT ParseAtomUnknownElement(
     }
     AtomExitOnFailure(hr, "Failed to enumerate all attributes on ATOM unknown element.");
 
-    ATOM_UNKNOWN_ELEMENT** ppTail = ppUnknownElement;
     while (*ppTail)
     {
         ppTail = &(*ppTail)->pNext;
@@ -1010,6 +1010,7 @@ static HRESULT ParseAtomUnknownAttribute(
     BSTR bstrNodeName = NULL;
     BSTR bstrNodeValue = NULL;
     ATOM_UNKNOWN_ATTRIBUTE* pNewUnknownAttribute;
+    ATOM_UNKNOWN_ATTRIBUTE** ppTail = ppUnknownAttribute;
 
     pNewUnknownAttribute = (ATOM_UNKNOWN_ATTRIBUTE*)MemAlloc(sizeof(ATOM_UNKNOWN_ATTRIBUTE), TRUE);
     AtomExitOnNull(pNewUnknownAttribute, hr, E_OUTOFMEMORY, "Failed to allocate unknown attribute.");
@@ -1038,7 +1039,6 @@ static HRESULT ParseAtomUnknownAttribute(
     hr = StrAllocString(&pNewUnknownAttribute->wzValue, bstrNodeValue, 0);
     AtomExitOnFailure(hr, "Failed to allocate ATOM unknown attribute value.");
 
-    ATOM_UNKNOWN_ATTRIBUTE** ppTail = ppUnknownAttribute;
     while (*ppTail)
     {
         ppTail = &(*ppTail)->pNext;
diff --git a/src/libs/dutil/WixToolset.DUtil/cabcutil.cpp b/src/libs/dutil/WixToolset.DUtil/cabcutil.cpp
index 294669af..e8401045 100644
--- a/src/libs/dutil/WixToolset.DUtil/cabcutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/cabcutil.cpp
@@ -200,6 +200,8 @@ extern "C" HRESULT DAPI CabCBegin(
 
     C_ASSERT(sizeof(MSIFILEHASHINFO) == 20);
 
+    size_t cbFileAllocSize = 0;
+
     LPWSTR pwzPathBuffer = NULL;
     if (wzCabDir)
     {
@@ -221,7 +223,7 @@ extern "C" HRESULT DAPI CabCBegin(
 
     pcd->fileSplitCabNamesCallback = NULL;
 
-    if (NULL == dwMaxSize)
+    if (0 == dwMaxSize)
     {
         pcd->ccab.cb = CAB_MAX_SIZE;
         pcd->fCabinetSplittingEnabled = FALSE; // If no max cab size is supplied, cabinet splitting is not desired
@@ -302,7 +304,6 @@ extern "C" HRESULT DAPI CabCBegin(
     }
 
     pcd->cMaxFilePaths = dwMaxFiles;
-    size_t cbFileAllocSize = 0;
 
     hr = ::SizeTMult(pcd->cMaxFilePaths, sizeof(CABC_FILE), &(cbFileAllocSize));
     CabcExitOnFailure(hr, "Maximum allocation exceeded on initialization.");
@@ -826,6 +827,7 @@ static HRESULT AddNonDuplicateFile(
 {
     HRESULT hr = S_OK;
     LPVOID pv = NULL;
+    CABC_FILE *pcf = NULL;
 
     // Ensure there is enough memory to store this file index.
     if (pcd->cFilePaths == pcd->cMaxFilePaths)
@@ -847,7 +849,7 @@ static HRESULT AddNonDuplicateFile(
 
     // Store the file index information.
     // TODO: add this to a sorted list so we can do a binary search later.
-    CABC_FILE *pcf = pcd->prgFiles + pcd->cFilePaths;
+    pcf = pcd->prgFiles + pcd->cFilePaths;
     pcf->dwCabFileIndex = dwCabFileIndex;
     pcf->llFileSize = llFileSize;
 
@@ -1366,6 +1368,7 @@ static __callback BOOL DIAMONDAPI CabCGetNextCabinet(
     HRESULT hr = S_OK;
     LPWSTR pwzFileToken = NULL;
     WCHAR wzNewCabName[MAX_PATH] = L"";
+    const int nAlphabets = 26;
 
     if (pccab->iCab == 1)
     {
@@ -1380,7 +1383,6 @@ static __callback BOOL DIAMONDAPI CabCGetNextCabinet(
         CabcExitOnFailure(hr, "Failed to remove extension to create next Cabinet File Name");
     }
 
-    const int nAlphabets = 26; // Number of Alphabets from a to z
     if (pccab->iCab <= nAlphabets)
     {
         // Construct next cab names like cab1a.cab, cab1b.cab, cab1c.cab, ........
diff --git a/src/libs/dutil/WixToolset.DUtil/cabutil.cpp b/src/libs/dutil/WixToolset.DUtil/cabutil.cpp
index 57463e1a..eac78b60 100644
--- a/src/libs/dutil/WixToolset.DUtil/cabutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/cabutil.cpp
@@ -574,7 +574,7 @@ static __callback INT_PTR DIAMONDAPI CabExtractCallback(__in FDINOTIFICATIONTYPE
         hr = StrAllocStringAnsi(&pwz, sz, 0, CP_ACP);
         CabExitOnFailure(hr, "failed to convert cabinet file id to unicode: %hs", sz);
 
-        if (NULL != pFDINotify->hf)  // just close the file
+        if (0 != pFDINotify->hf)  // just close the file
         {
             ::CloseHandle(reinterpret_cast<HANDLE>(pFDINotify->hf));
         }
diff --git a/src/libs/dutil/WixToolset.DUtil/certutil.cpp b/src/libs/dutil/WixToolset.DUtil/certutil.cpp
index 69897b9e..0b1dec77 100644
--- a/src/libs/dutil/WixToolset.DUtil/certutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/certutil.cpp
@@ -248,7 +248,7 @@ extern "C" BOOL DAPI CertHasPrivateKey(
     __in PCCERT_CONTEXT pCertContext,
     __out_opt DWORD* pdwKeySpec)
 {
-    HCRYPTPROV_OR_NCRYPT_KEY_HANDLE hPrivateKey = NULL;
+    HCRYPTPROV_OR_NCRYPT_KEY_HANDLE hPrivateKey = 0;
     DWORD dwKeySpec = 0;
     // set CRYPT_ACQUIRE_CACHE_FLAG so that we don't have to release the private key handle
     BOOL fResult = ::CryptAcquireCertificatePrivateKey(
@@ -278,7 +278,7 @@ extern "C" HRESULT DAPI CertInstallSingleCertificate(
 
     DWORD dwKeySpec = 0;
 
-    HCRYPTPROV hCsp = NULL;
+    HCRYPTPROV hCsp = 0;
     LPWSTR pwszTmpContainer = NULL;
     LPWSTR pwszProviderName = NULL;
     DWORD dwProviderType = 0;
diff --git a/src/libs/dutil/WixToolset.DUtil/conutil.cpp b/src/libs/dutil/WixToolset.DUtil/conutil.cpp
index 059af0e0..c5a064b5 100644
--- a/src/libs/dutil/WixToolset.DUtil/conutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/conutil.cpp
@@ -379,8 +379,6 @@ extern "C" HRESULT DAPI ConsoleReadNonBlockingW(
 
     LPSTR psz = NULL;
 
-    ConExitOnNull(ppwzBuffer, hr, E_INVALIDARG, "Failed to read from console because buffer was not provided");
-
     DWORD dwRead;
     DWORD dwNumInput;
 
@@ -393,6 +391,8 @@ extern "C" HRESULT DAPI ConsoleReadNonBlockingW(
     INPUT_RECORD ir;
     WCHAR chIn;
 
+    ConExitOnNull(ppwzBuffer, hr, E_INVALIDARG, "Failed to read from console because buffer was not provided");
+
     *ppwzBuffer = NULL;
     *pcchSize = 0;
 
diff --git a/src/libs/dutil/WixToolset.DUtil/cryputil.cpp b/src/libs/dutil/WixToolset.DUtil/cryputil.cpp
index 24bb83f1..c12519df 100644
--- a/src/libs/dutil/WixToolset.DUtil/cryputil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/cryputil.cpp
@@ -214,8 +214,8 @@ extern "C" HRESULT DAPI CrypHashFileHandle(
     )
 {
     HRESULT hr = S_OK;
-    HCRYPTPROV hProv = NULL;
-    HCRYPTHASH hHash = NULL;
+    HCRYPTPROV hProv = 0;
+    HCRYPTHASH hHash = 0;
     DWORD cbRead = 0;
     BYTE rgbBuffer[4096] = { };
     const LARGE_INTEGER liZero = { };
@@ -289,8 +289,8 @@ HRESULT DAPI CrypHashBuffer(
     )
 {
     HRESULT hr = S_OK;
-    HCRYPTPROV hProv = NULL;
-    HCRYPTHASH hHash = NULL;
+    HCRYPTPROV hProv = 0;
+    HCRYPTHASH hHash = 0;
     DWORD cbDataHashed = 0;
     SIZE_T cbTotal = 0;
     SIZE_T cbRemaining = 0;
diff --git a/src/libs/dutil/WixToolset.DUtil/dictutil.cpp b/src/libs/dutil/WixToolset.DUtil/dictutil.cpp
index 09b150df..88c35cf6 100644
--- a/src/libs/dutil/WixToolset.DUtil/dictutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/dictutil.cpp
@@ -339,11 +339,12 @@ extern "C" HRESULT DAPI DictGetValue(
     )
 {
     HRESULT hr = S_OK;
+    const STRINGDICT_STRUCT *psd = NULL;
 
     DictExitOnNull(sdHandle, hr, E_INVALIDARG, "Handle not specified while searching dict");
     DictExitOnNull(pszString, hr, E_INVALIDARG, "String not specified while searching dict");
 
-    const STRINGDICT_STRUCT *psd = static_cast<const STRINGDICT_STRUCT *>(sdHandle);
+    psd = static_cast<const STRINGDICT_STRUCT *>(sdHandle);
 
     if (DICT_EMBEDDED_KEY != psd->dtType)
     {
@@ -368,11 +369,12 @@ extern "C" HRESULT DAPI DictKeyExists(
     )
 {
     HRESULT hr = S_OK;
+    const STRINGDICT_STRUCT *psd = NULL;
 
     DictExitOnNull(sdHandle, hr, E_INVALIDARG, "Handle not specified while searching dict");
     DictExitOnNull(pszString, hr, E_INVALIDARG, "String not specified while searching dict");
 
-    const STRINGDICT_STRUCT *psd = static_cast<const STRINGDICT_STRUCT *>(sdHandle);
+    psd = static_cast<const STRINGDICT_STRUCT *>(sdHandle);
 
     // This works with either type of dictionary
     hr = GetValue(psd, pszString, NULL);
@@ -417,6 +419,7 @@ static HRESULT CreateDict(
     )
 {
     HRESULT hr = S_OK;
+    STRINGDICT_STRUCT *psd = NULL;
 
     DictExitOnNull(psdHandle, hr, E_INVALIDARG, "Handle not specified while creating dict.");
 
@@ -424,7 +427,7 @@ static HRESULT CreateDict(
     *psdHandle = static_cast<STRINGDICT_HANDLE>(MemAlloc(sizeof(STRINGDICT_STRUCT), TRUE));
     DictExitOnNull(*psdHandle, hr, E_OUTOFMEMORY, "Failed to allocate dictionary object.");
 
-    STRINGDICT_STRUCT* psd = static_cast<STRINGDICT_STRUCT*>(*psdHandle);
+    psd = static_cast<STRINGDICT_STRUCT*>(*psdHandle);
 
     // Fill out the new handle's values
     psd->dtType = dtType;
@@ -524,6 +527,7 @@ static HRESULT GetValue(
     DWORD dwOriginalIndexCandidate = 0;
     void *pvCandidateValue = NULL;
     DWORD dwIndex = 0;
+    DWORD dwIndexCandidate = 0;
 
     DictExitOnNull(psd, hr, E_INVALIDARG, "Handle not specified while searching dict");
     DictExitOnNull(pszString, hr, E_INVALIDARG, "String not specified while searching dict");
@@ -537,7 +541,7 @@ static HRESULT GetValue(
     hr = StringHash(psd, MAX_BUCKET_SIZES[psd->dwBucketSizeIndex], pszString, &dwOriginalIndexCandidate);
     DictExitOnFailure(hr, "Failed to hash the string.");
 
-    DWORD dwIndexCandidate = dwOriginalIndexCandidate;
+    dwIndexCandidate = dwOriginalIndexCandidate;
 
     pvCandidateValue = TranslateOffsetToValue(psd, psd->ppvBuckets[dwIndexCandidate]);
 
@@ -582,11 +586,12 @@ static HRESULT GetInsertIndex(
 {
     HRESULT hr = S_OK;
     DWORD dwOriginalIndexCandidate = 0;
+    DWORD dwIndexCandidate = 0;
 
     hr = StringHash(psd, dwBucketCount, pszString, &dwOriginalIndexCandidate);
     DictExitOnFailure(hr, "Failed to hash the string.");
 
-    DWORD dwIndexCandidate = dwOriginalIndexCandidate;
+    dwIndexCandidate = dwOriginalIndexCandidate;
 
     // If we collide, keep iterating forward from our intended position, even wrapping around to zero, until we find an empty bucket
 #pragma prefast(push)
@@ -625,6 +630,7 @@ static HRESULT GetIndex(
 {
     HRESULT hr = S_OK;
     DWORD dwOriginalIndexCandidate = 0;
+    DWORD dwIndexCandidate = 0;
 
     if (psd->dwBucketSizeIndex >= countof(MAX_BUCKET_SIZES))
     {
@@ -635,7 +641,7 @@ static HRESULT GetIndex(
     hr = StringHash(psd, MAX_BUCKET_SIZES[psd->dwBucketSizeIndex], pszString, &dwOriginalIndexCandidate);
     DictExitOnFailure(hr, "Failed to hash the string.");
 
-    DWORD dwIndexCandidate = dwOriginalIndexCandidate;
+    dwIndexCandidate = dwOriginalIndexCandidate;
 
     while (!IsMatchExact(psd, dwIndexCandidate, pszString))
     {
diff --git a/src/libs/dutil/WixToolset.DUtil/dlutil.cpp b/src/libs/dutil/WixToolset.DUtil/dlutil.cpp
index 9c704ee8..8b589a31 100644
--- a/src/libs/dutil/WixToolset.DUtil/dlutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/dlutil.cpp
@@ -624,7 +624,7 @@ static HRESULT OpenRequest(
     }
 
     // Open the request and add the header if provided.
-    hUrl = ::HttpOpenRequestW(hConnect, wzMethod, sczResource, NULL, NULL, DOWNLOAD_ENGINE_ACCEPT_TYPES, dwRequestFlags, NULL);
+    hUrl = ::HttpOpenRequestW(hConnect, wzMethod, sczResource, NULL, NULL, DOWNLOAD_ENGINE_ACCEPT_TYPES, dwRequestFlags, 0);
     DlExitOnNullWithLastError(hUrl, hr, "Failed to open internet request.");
 
     if (wzHeader && *wzHeader)
diff --git a/src/libs/dutil/WixToolset.DUtil/eseutil.cpp b/src/libs/dutil/WixToolset.DUtil/eseutil.cpp
index b9455d4b..f9871d99 100644
--- a/src/libs/dutil/WixToolset.DUtil/eseutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/eseutil.cpp
@@ -105,8 +105,8 @@ HRESULT HresultFromJetError(JET_ERR jEr)
     return hr;
 }
 
-#define ExitOnJetFailure(e, x, s, ...) { x = HresultFromJetError(e); if (S_OK != x) { ExitTraceSource(DUTIL_SOURCE_ESEUTIL, x, s, __VA_ARGS__); goto LExit; }}
-#define ExitOnRootJetFailure(e, x, s, ...) { x = HresultFromJetError(e); if (S_OK != x) { Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceSource(DUTIL_SOURCE_ESEUTIL, x, s, __VA_ARGS__); goto LExit; }}
+#define ExitOnJetFailure(e, x, s, ...) { x = HresultFromJetError(e); if (S_OK != x) { ExitTraceSource(DUTIL_SOURCE_ESEUTIL, x, s __VA_OPT__(,) __VA_ARGS__); goto LExit; }}
+#define ExitOnRootJetFailure(e, x, s, ...) { x = HresultFromJetError(e); if (S_OK != x) { Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceSource(DUTIL_SOURCE_ESEUTIL, x, s __VA_OPT__(,) __VA_ARGS__); goto LExit; }}
 
 HRESULT DAPI EseBeginSession(
     __out JET_INSTANCE *pjiInstance,
@@ -134,14 +134,14 @@ HRESULT DAPI EseBeginSession(
     jEr = JetCreateInstanceA(pjiInstance, pszAnsiInstance);
     ExitOnJetFailure(jEr, hr, "Failed to create instance");
 
-    jEr = JetSetSystemParameter(pjiInstance, NULL, JET_paramSystemPath, NULL, pszAnsiPath);
+    jEr = JetSetSystemParameter(pjiInstance, 0, JET_paramSystemPath, 0, pszAnsiPath);
     ExitOnJetFailure(jEr, hr, "Failed to set jet system path to: %s", pszAnsiPath);
 
     // This makes sure log files that are created are created next to the database, not next to our EXE (note they last after execution)
-    jEr = JetSetSystemParameter(pjiInstance, NULL, JET_paramLogFilePath, NULL, pszAnsiPath);
+    jEr = JetSetSystemParameter(pjiInstance, 0, JET_paramLogFilePath, 0, pszAnsiPath);
     ExitOnJetFailure(jEr, hr, "Failed to set jet log file path to: %s", pszAnsiPath);
 
-    jEr = JetSetSystemParameter(pjiInstance, NULL, JET_paramMaxOpenTables, 10, NULL);
+    jEr = JetSetSystemParameter(pjiInstance, 0, JET_paramMaxOpenTables, 10, 0);
     ExitOnJetFailure(jEr, hr, "Failed to set jet max open tables parameter");
 
     // TODO: Use callback hooks so that Jet Engine uses our memory allocation methods, etc.? (search docs for "JET_PFNREALLOC" - there are other callbacks too)
@@ -269,6 +269,7 @@ HRESULT AllocIndexCreateStruct(
     LPSTR pszIndexName = NULL;
     LPSTR pszTempString = NULL;
     BOOL fKeyColumns = FALSE;
+    size_t cchSize = 0;
     DWORD_PTR i;
 
     for (i=0; i < ptsSchema->dwColumns; ++i)
@@ -312,7 +313,6 @@ HRESULT AllocIndexCreateStruct(
     EseExitOnNull(*ppjicIndexCreate, hr, E_OUTOFMEMORY, "Failed to allocate index create structure for database");
 
     // Record the size including both null terminators - the struct requires this
-    size_t cchSize = 0;
     hr = ::StringCchLengthA(pszMultiSzKeys, STRSAFE_MAX_LENGTH, &cchSize);
     EseExitOnRootFailure(hr, "Failed to get size of keys string");
 
@@ -389,7 +389,7 @@ HRESULT EnsureSchema(
             EseExitOnFailure(hr, "Failed to allocate index create struct");
 
             jtTableCreate.cColumns = pdsSchema->ptsTables[dwTable].dwColumns;
-            jtTableCreate.tableid = NULL;
+            jtTableCreate.tableid = 0;
 
             // TODO: Investigate why we can't create a table without a key column?
             // Actually create the table using our JET_TABLECREATE struct
@@ -1047,11 +1047,12 @@ HRESULT DAPI EseBeginQuery(
     UNREFERENCED_PARAMETER(jtTable);
 
     HRESULT hr = S_OK;
+    ESE_QUERY *peqHandle = NULL;
 
     *peqhHandle = static_cast<ESE_QUERY*>(MemAlloc(sizeof(ESE_QUERY), TRUE));
     EseExitOnNull(*peqhHandle, hr, E_OUTOFMEMORY, "Failed to allocate new query");
 
-    ESE_QUERY *peqHandle = static_cast<ESE_QUERY *>(*peqhHandle);
+    peqHandle = static_cast<ESE_QUERY *>(*peqhHandle);
     peqHandle->qtQueryType = qtQueryType;
     peqHandle->jsSession = jsSession;
     peqHandle->jtTable = jtTable;
diff --git a/src/libs/dutil/WixToolset.DUtil/fileutil.cpp b/src/libs/dutil/WixToolset.DUtil/fileutil.cpp
index abe1f6ad..7afda70f 100644
--- a/src/libs/dutil/WixToolset.DUtil/fileutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/fileutil.cpp
@@ -48,7 +48,7 @@ __out LPWSTR *ppwzFileNameNoExtension
     FileExitOnFailure(hr, "failed to allocate space for File Name without extension");
 
     // _wsplitpath_s can handle drive/path/filename/extension
-    err = _wsplitpath_s(wzFileName, NULL, NULL, NULL, NULL, pwzFileNameNoExtension, cchFileNameNoExtension, NULL, NULL);
+    err = _wsplitpath_s(wzFileName, NULL, 0, NULL, 0, pwzFileNameNoExtension, cchFileNameNoExtension, NULL, 0);
     if (err)
     {
         hr = E_INVALIDARG;
@@ -110,11 +110,12 @@ extern "C" HRESULT DAPI FileAddSuffixToBaseName(
     HRESULT hr = S_OK;
     LPWSTR sczNewFileName = NULL;
     size_t cchFileName = 0;
+    LPCWSTR wzExtension = 0;
 
     hr = ::StringCchLengthW(wzFileName, STRSAFE_MAX_CCH, &cchFileName);
     FileExitOnRootFailure(hr, "Failed to get length of file name: %ls", wzFileName);
 
-    LPCWSTR wzExtension = wzFileName + cchFileName;
+    wzExtension = wzFileName + cchFileName;
     while (wzFileName < wzExtension && L'.' != *wzExtension)
     {
         --wzExtension;
@@ -613,6 +614,8 @@ extern "C" HRESULT DAPI FileReadPartialEx(
     LARGE_INTEGER liFileSize = { };
     DWORD cbData = 0;
     BYTE* pbData = NULL;
+    DWORD cbTotalRead = 0;
+    DWORD cbRead = 0;
 
     FileExitOnNull(pcbDest, hr, E_INVALIDARG, "Invalid argument pcbDest");
     FileExitOnNull(ppbDest, hr, E_INVALIDARG, "Invalid argument ppbDest");
@@ -694,8 +697,6 @@ extern "C" HRESULT DAPI FileReadPartialEx(
         FileExitOnNull(pbData, hr, E_OUTOFMEMORY, "Failed to allocate memory to read in file: %ls", wzSrcPath);
     }
 
-    DWORD cbTotalRead = 0;
-    DWORD cbRead = 0;
     do
     {
         DWORD cbRemaining = 0;
diff --git a/src/libs/dutil/WixToolset.DUtil/iis7util.cpp b/src/libs/dutil/WixToolset.DUtil/iis7util.cpp
index d0a0b000..27a95766 100644
--- a/src/libs/dutil/WixToolset.DUtil/iis7util.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/iis7util.cpp
@@ -1,9 +1,10 @@
 // Copyright (c) .NET Foundation and contributors. All rights reserved. Licensed under the Microsoft Reciprocal License. See LICENSE.TXT file in the project root for full license information.
 
+#if 0
+
 #include "precomp.h"
 #include "iis7util.h"
 
-
 // Exit macros
 #define IisExitOnLastError(x, s, ...) ExitOnLastErrorSource(DUTIL_SOURCE_IIS7UTIL, x, s, __VA_ARGS__)
 #define IisExitOnLastErrorDebugTrace(x, s, ...) ExitOnLastErrorDebugTraceSource(DUTIL_SOURCE_IIS7UTIL, x, s, __VA_ARGS__)
@@ -533,3 +534,5 @@ LExit:
 
     return hr;
 }
+
+#endif
diff --git a/src/libs/dutil/WixToolset.DUtil/inc/cabutil.h b/src/libs/dutil/WixToolset.DUtil/inc/cabutil.h
index 0bedba80..93ed7728 100644
--- a/src/libs/dutil/WixToolset.DUtil/inc/cabutil.h
+++ b/src/libs/dutil/WixToolset.DUtil/inc/cabutil.h
@@ -3,7 +3,7 @@
 
 
 #include <fdi.h>
-#include <sys\stat.h>
+#include <sys/stat.h>
 
 #ifdef __cplusplus
 extern "C" {
diff --git a/src/libs/dutil/WixToolset.DUtil/inc/conutil.h b/src/libs/dutil/WixToolset.DUtil/inc/conutil.h
index 84038d1f..a1659609 100644
--- a/src/libs/dutil/WixToolset.DUtil/inc/conutil.h
+++ b/src/libs/dutil/WixToolset.DUtil/inc/conutil.h
@@ -6,18 +6,18 @@
 extern "C" {
 #endif
 
-#define ConsoleExitOnFailureSource(d, x, c, f, ...) if (FAILED(x)) { ConsoleWriteError(x, c, f, __VA_ARGS__); ExitTraceSource(d, x, f, __VA_ARGS__); goto LExit; }
-#define ConsoleExitOnLastErrorSource(d, x, c, f, ...) { x = ::GetLastError(); x = HRESULT_FROM_WIN32(x); if (FAILED(x)) { ConsoleWriteError(x, c, f, __VA_ARGS__); ExitTraceSource(d, x, f, __VA_ARGS__); goto LExit; } }
-#define ConsoleExitOnNullSource(d, p, x, e, c, f, ...) if (NULL == p) { x = e; ConsoleWriteError(x, c, f, __VA_ARGS__); ExitTraceSource(d, x, f, __VA_ARGS__); goto LExit; }
-#define ConsoleExitOnNullWithLastErrorSource(d, p, x, c, f, ...) if (NULL == p) { DWORD Dutil_er = ::GetLastError(); x = HRESULT_FROM_WIN32(Dutil_er); if (!FAILED(x)) { x = E_FAIL; } ConsoleWriteError(x, c, f, __VA_ARGS__); ExitTraceSource(d, x, f, __VA_ARGS__); goto LExit; }
-#define ConsoleExitWithLastErrorSource(d, x, c, f, ...) { DWORD Dutil_er = ::GetLastError(); x = HRESULT_FROM_WIN32(Dutil_er); if (!FAILED(x)) { x = E_FAIL; } ConsoleWriteError(x, c, f, __VA_ARGS__); ExitTraceSource(d, x, f, __VA_ARGS__); goto LExit; }
-
-
-#define ConsoleExitOnFailure(x, c, f, ...) ConsoleExitOnFailureSource(DUTIL_SOURCE_DEFAULT, x, c, f, __VA_ARGS__)
-#define ConsoleExitOnLastError(x, c, f, ...) ConsoleExitOnLastErrorSource(DUTIL_SOURCE_DEFAULT, x, c, f, __VA_ARGS__)
-#define ConsoleExitOnNull(p, x, e, c, f, ...) ConsoleExitOnNullSource(DUTIL_SOURCE_DEFAULT, p, x, e, c, f, __VA_ARGS__)
-#define ConsoleExitOnNullWithLastError(p, x, c, f, ...) ConsoleExitOnNullWithLastErrorSource(DUTIL_SOURCE_DEFAULT, p, x, c, f, __VA_ARGS__)
-#define ConsoleExitWithLastError(x, c, f, ...) ConsoleExitWithLastErrorSource(DUTIL_SOURCE_DEFAULT, x, c, f, __VA_ARGS__)
+#define ConsoleExitOnFailureSource(d, x, c, f, ...) if (FAILED(x)) { ConsoleWriteError(x, c, f __VA_OPT__(,) __VA_ARGS__); ExitTraceSource(d, x, f __VA_OPT__(,) __VA_ARGS__); goto LExit; }
+#define ConsoleExitOnLastErrorSource(d, x, c, f, ...) { x = ::GetLastError(); x = HRESULT_FROM_WIN32(x); if (FAILED(x)) { ConsoleWriteError(x, c, f, __VA_ARGS__); ExitTraceSource(d, x, f __VA_OPT__(,) __VA_ARGS__); goto LExit; } }
+#define ConsoleExitOnNullSource(d, p, x, e, c, f, ...) if (NULL == p) { x = e; ConsoleWriteError(x, c, f, __VA_ARGS__); ExitTraceSource(d, x, f __VA_OPT__(,) __VA_ARGS__); goto LExit; }
+#define ConsoleExitOnNullWithLastErrorSource(d, p, x, c, f, ...) if (NULL == p) { DWORD Dutil_er = ::GetLastError(); x = HRESULT_FROM_WIN32(Dutil_er); if (!FAILED(x)) { x = E_FAIL; } ConsoleWriteError(x, c, f, __VA_ARGS__); ExitTraceSource(d, x, f __VA_OPT__(,) __VA_ARGS__); goto LExit; }
+#define ConsoleExitWithLastErrorSource(d, x, c, f, ...) { DWORD Dutil_er = ::GetLastError(); x = HRESULT_FROM_WIN32(Dutil_er); if (!FAILED(x)) { x = E_FAIL; } ConsoleWriteError(x, c, f, __VA_ARGS__); ExitTraceSource(d, x, f __VA_OPT__(,) __VA_ARGS__); goto LExit; }
+
+
+#define ConsoleExitOnFailure(x, c, f, ...) ConsoleExitOnFailureSource(DUTIL_SOURCE_DEFAULT, x, c, f __VA_OPT__(,) __VA_ARGS__)
+#define ConsoleExitOnLastError(x, c, f, ...) ConsoleExitOnLastErrorSource(DUTIL_SOURCE_DEFAULT, x, c, f __VA_OPT__(,) __VA_ARGS__)
+#define ConsoleExitOnNull(p, x, e, c, f, ...) ConsoleExitOnNullSource(DUTIL_SOURCE_DEFAULT, p, x, e, c, f __VA_OPT__(,) __VA_ARGS__)
+#define ConsoleExitOnNullWithLastError(p, x, c, f, ...) ConsoleExitOnNullWithLastErrorSource(DUTIL_SOURCE_DEFAULT, p, x, c, f __VA_OPT__(,) __VA_ARGS__)
+#define ConsoleExitWithLastError(x, c, f, ...) ConsoleExitWithLastErrorSource(DUTIL_SOURCE_DEFAULT, x, c, f __VA_OPT__(,) __VA_ARGS__)
 
 // enums
 typedef enum CONSOLE_COLOR { CONSOLE_COLOR_NORMAL, CONSOLE_COLOR_RED, CONSOLE_COLOR_YELLOW, CONSOLE_COLOR_GREEN } CONSOLE_COLOR;
diff --git a/src/libs/dutil/WixToolset.DUtil/inc/dutil.h b/src/libs/dutil/WixToolset.DUtil/inc/dutil.h
index 7d8c7d81..0dbffcf7 100644
--- a/src/libs/dutil/WixToolset.DUtil/inc/dutil.h
+++ b/src/libs/dutil/WixToolset.DUtil/inc/dutil.h
@@ -3,6 +3,49 @@
 
 #include "dutilsources.h"
 
+#include <sal.h>
+#define __in
+#define __in_bcount_opt(b)
+#define __in_z
+#define __in_z_opt
+#define __in_ecount_opt(b)
+#define __in_xcount(b)
+#define __out
+#define __out_bcount_part_opt(x,y)
+#define __out_data_source(x)
+#define __out_ecount_opt(b)
+#define __out_range(x,y)
+#define __out_z
+#define __out_z_opt
+#define __inout_opt
+#define __inout_z
+#define __inout_z_opt
+#define __deref_opt_out_z
+#define __deref_opt_out_z_opt
+#define __deref_out_bcount(b)
+#define __deref_out_bcount_full(b)
+#define __deref_out_bcount_opt(b)
+#define __deref_out_bound
+#define __deref_out_ecount_opt(b)
+#define __deref_out_ecount_part(x,y)
+#define __deref_out_ecount_z(x)
+#define __deref_out_z
+#define __deref_out_z_opt
+#define __deref_inout
+#define __deref_inout_bcount(b)
+#define __deref_inout_bcount_full(b)
+#define __deref_inout_ecount(b)
+#define __deref_inout_ecount_opt(x)
+#define __deref_inout_ecount_part(x,y)
+#define __deref_inout_z
+#define __deref_inout_z_opt
+#define __format_string
+#define __nullnullterminated
+#define __reserved
+#define __success(e)
+#define __callback
+#define __assume(e)
+
 #define DAPI __stdcall
 #define DAPIV __cdecl // used only for functions taking variable length arguments
 
@@ -120,8 +163,8 @@ void DAPI Dutil_RootFailure(__in_z LPCSTR szFile, __in int iLine, __in HRESULT h
 
 #define ExitFunctionWithLastError(x) { x = HRESULT_FROM_WIN32(::GetLastError()); goto LExit; }
 
-#define ExitTraceSource(d, x, s, ...) { TraceError(x, s, __VA_ARGS__); (void)Dutil_TraceErrorSource(__FILE__, __LINE__, REPORT_ERROR, d, x, s, __VA_ARGS__); }
-#define ExitTraceDebugSource(d, x, s, ...) { TraceErrorDebug(x, s, __VA_ARGS__); (void)Dutil_TraceErrorSource(__FILE__, __LINE__, REPORT_DEBUG, d, x, s, __VA_ARGS__); }
+#define ExitTraceSource(d, x, s, ...) { TraceError(x, s __VA_OPT__(,) __VA_ARGS__); (void)Dutil_TraceErrorSource(__FILE__, __LINE__, REPORT_ERROR, d, x, s __VA_OPT__(,) __VA_ARGS__); }
+#define ExitTraceDebugSource(d, x, s, ...) { TraceErrorDebug(x, s, __VA_ARGS__); (void)Dutil_TraceErrorSource(__FILE__, __LINE__, REPORT_DEBUG, d, x, s __VA_OPT__(,) __VA_ARGS__); }
 
 #define ExitOnLastErrorSource(d, x, s, ...) { DWORD Dutil_er = ::GetLastError(); x = HRESULT_FROM_WIN32(Dutil_er); if (FAILED(x)) { Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceSource(d, x, s, __VA_ARGS__); goto LExit; } }
 #define ExitOnLastErrorDebugTraceSource(d, x, s, ...) { DWORD Dutil_er = ::GetLastError(); x = HRESULT_FROM_WIN32(Dutil_er); if (FAILED(x)) { Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceDebugSource(d, x, s, __VA_ARGS__); goto LExit; } }
@@ -130,9 +173,9 @@ void DAPI Dutil_RootFailure(__in_z LPCSTR szFile, __in int iLine, __in HRESULT h
 #define ExitOnRootFailureSource(d, x, s, ...) if (FAILED(x)) { Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceSource(d, x, s, __VA_ARGS__);  goto LExit; }
 #define ExitWithRootFailureSource(d, x, e, s, ...) { x = FAILED(e) ? e : E_FAIL; Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceSource(d, x, s, __VA_ARGS__); goto LExit; }
 #define ExitOnFailureDebugTraceSource(d, x, s, ...) if (FAILED(x)) { ExitTraceDebugSource(d, x, s, __VA_ARGS__);  goto LExit; }
-#define ExitOnNullSource(d, p, x, e, s, ...) if (NULL == p) { x = e; Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceSource(d, x, s, __VA_ARGS__);  goto LExit; }
-#define ExitOnNullWithLastErrorSource(d, p, x, s, ...) if (NULL == p) { DWORD Dutil_er = ::GetLastError(); x = HRESULT_FROM_WIN32(Dutil_er); if (!FAILED(x)) { x = E_FAIL; } Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceSource(d, x, s, __VA_ARGS__); goto LExit; }
-#define ExitOnNullDebugTraceSource(d, p, x, e, s, ...) if (NULL == p) { x = e; Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceDebugSource(d, x, s, __VA_ARGS__);  goto LExit; }
+#define ExitOnNullSource(d, p, x, e, s, ...) if (0 == p) { x = e; Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceSource(d, x, s __VA_OPT__(,) __VA_ARGS__);  goto LExit; }
+#define ExitOnNullWithLastErrorSource(d, p, x, s, ...) if (0 == p) { DWORD Dutil_er = ::GetLastError(); x = HRESULT_FROM_WIN32(Dutil_er); if (!FAILED(x)) { x = E_FAIL; } Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceSource(d, x, s __VA_OPT__(,) __VA_ARGS__); goto LExit; }
+#define ExitOnNullDebugTraceSource(d, p, x, e, s, ...) if (0 == p) { x = e; Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceDebugSource(d, x, s __VA_OPT__(,) __VA_ARGS__);  goto LExit; }
 #define ExitOnInvalidHandleWithLastErrorSource(d, p, x, s, ...) if (INVALID_HANDLE_VALUE == p) { DWORD Dutil_er = ::GetLastError(); x = HRESULT_FROM_WIN32(Dutil_er); if (!FAILED(x)) { x = E_FAIL; } Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceSource(d, x, s, __VA_ARGS__); goto LExit; }
 #define ExitOnWin32ErrorSource(d, e, x, s, ...) if (ERROR_SUCCESS != e) { x = HRESULT_FROM_WIN32(e); if (!FAILED(x)) { x = E_FAIL; } Dutil_RootFailure(__FILE__, __LINE__, x); ExitTraceSource(d, x, s, __VA_ARGS__); goto LExit; }
 #define ExitOnOptionalXmlQueryFailureSource(d, x, b, s, ...) { { if (S_FALSE == x || E_NOTFOUND == x) { b = FALSE; x = S_OK; } else { b = SUCCEEDED(x); } }; ExitOnRootFailureSource(d, x, s, __VA_ARGS__); }
diff --git a/src/libs/dutil/WixToolset.DUtil/inc/iis7util.h b/src/libs/dutil/WixToolset.DUtil/inc/iis7util.h
index 3572b4e9..00f286a8 100644
--- a/src/libs/dutil/WixToolset.DUtil/inc/iis7util.h
+++ b/src/libs/dutil/WixToolset.DUtil/inc/iis7util.h
@@ -115,8 +115,8 @@ extern "C" {
 #define IIS_CONFIG_HTTPLOGGING_SECTION      L"system.webServer/httpLogging"
 #define IIS_CONFIG_DONTLOG                  L"dontLog"
 
-typedef BOOL (CALLBACK* ENUMAPHOSTELEMENTPROC)(IAppHostElement*, LPVOID);
-typedef BOOL (CALLBACK* VARIANTCOMPARATORPROC)(VARIANT*, VARIANT*);
+typedef BOOL (*ENUMAPHOSTELEMENTPROC)(void *, LPVOID);
+typedef BOOL (*VARIANTCOMPARATORPROC)(VARIANT*, VARIANT*);
 
 HRESULT DAPI Iis7PutPropertyVariant(
     __in IAppHostElement *pElement,
diff --git a/src/libs/dutil/WixToolset.DUtil/inc/monutil.h b/src/libs/dutil/WixToolset.DUtil/inc/monutil.h
index f644e205..1deca578 100644
--- a/src/libs/dutil/WixToolset.DUtil/inc/monutil.h
+++ b/src/libs/dutil/WixToolset.DUtil/inc/monutil.h
@@ -12,8 +12,7 @@ extern "C" {
 typedef void* MON_HANDLE;
 typedef const void* C_MON_HANDLE;
 
-// Defined in regutil.h
-enum REG_KEY_BITNESS;
+#include "regutil.h"
 
 extern const int MON_HANDLE_BYTES;
 
diff --git a/src/libs/dutil/WixToolset.DUtil/inc/rexutil.h b/src/libs/dutil/WixToolset.DUtil/inc/rexutil.h
index 77f5604a..cb943499 100644
--- a/src/libs/dutil/WixToolset.DUtil/inc/rexutil.h
+++ b/src/libs/dutil/WixToolset.DUtil/inc/rexutil.h
@@ -2,7 +2,7 @@
 // Copyright (c) .NET Foundation and contributors. All rights reserved. Licensed under the Microsoft Reciprocal License. See LICENSE.TXT file in the project root for full license information.
 
 
-#include <sys\stat.h>
+#include <sys/stat.h>
 #include <fdi.h>
 
 #ifdef __cplusplus
diff --git a/src/libs/dutil/WixToolset.DUtil/inc/wiutil.h b/src/libs/dutil/WixToolset.DUtil/inc/wiutil.h
index 71f40e8a..f00a6617 100644
--- a/src/libs/dutil/WixToolset.DUtil/inc/wiutil.h
+++ b/src/libs/dutil/WixToolset.DUtil/inc/wiutil.h
@@ -1,6 +1,7 @@
 #pragma once
 // Copyright (c) .NET Foundation and contributors. All rights reserved. Licensed under the Microsoft Reciprocal License. See LICENSE.TXT file in the project root for full license information.
 
+#include <msi.h>
 
 #ifdef __cplusplus
 extern "C" {
diff --git a/src/libs/dutil/WixToolset.DUtil/iniutil.cpp b/src/libs/dutil/WixToolset.DUtil/iniutil.cpp
index 46f6e380..4a2b15bc 100644
--- a/src/libs/dutil/WixToolset.DUtil/iniutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/iniutil.cpp
@@ -599,6 +599,7 @@ extern "C" HRESULT DAPI IniWriteFile(
     LPCWSTR wzName = NULL;
     DWORD dwLineArrayIndex = 1;
     FILE_ENCODING feEncoding;
+    BOOL fSections = false;
 
     INI_STRUCT *pi = static_cast<INI_STRUCT *>(piHandle);
 
@@ -625,7 +626,7 @@ extern "C" HRESULT DAPI IniWriteFile(
         ExitFunction1(hr = E_NOTFOUND);
     }
 
-    BOOL fSections = (pi->sczOpenTagPrefix) && (pi->sczOpenTagPostfix);
+    fSections = (pi->sczOpenTagPrefix) && (pi->sczOpenTagPostfix);
 
     hr = StrAllocString(&sczContents, L"", 0);
     IniExitOnFailure(hr, "Failed to begin contents string as empty string");
diff --git a/src/libs/dutil/WixToolset.DUtil/jsonutil.cpp b/src/libs/dutil/WixToolset.DUtil/jsonutil.cpp
index 3450ba59..ded510a7 100644
--- a/src/libs/dutil/WixToolset.DUtil/jsonutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/jsonutil.cpp
@@ -598,6 +598,7 @@ static HRESULT SerializeJsonString(
 {
     HRESULT hr = S_OK;
     DWORD cchRequired = 3; // start with enough space for null terminated empty quoted string (aka: ""\0)
+    LPWSTR pchTarget = NULL;
 
     for (LPCWSTR pch = wzString; *pch; ++pch)
     {
@@ -613,7 +614,7 @@ static HRESULT SerializeJsonString(
     hr = StrAlloc(psczJsonString, cchRequired);
     JsonExitOnFailure(hr, "Failed to allocate space for JSON string.");
 
-    LPWSTR pchTarget = *psczJsonString;
+    pchTarget = *psczJsonString;
 
     *pchTarget = L'\"';
     ++pchTarget;
diff --git a/src/libs/dutil/WixToolset.DUtil/locutil.cpp b/src/libs/dutil/WixToolset.DUtil/locutil.cpp
index 008d0367..a9955efa 100644
--- a/src/libs/dutil/WixToolset.DUtil/locutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/locutil.cpp
@@ -51,7 +51,7 @@ static HRESULT ParseWxlControl(
 #ifndef MUI_MERGE_SYSTEM_FALLBACK
 #define MUI_MERGE_SYSTEM_FALLBACK           0x10     // GetThreadPreferredUILanguages merges in parent and base languages
 #endif
-typedef WINBASEAPI BOOL (WINAPI *GET_THREAD_PREFERRED_UI_LANGUAGES) (
+typedef BOOL (WINAPI *GET_THREAD_PREFERRED_UI_LANGUAGES) (
     __in DWORD dwFlags,
     __out PULONG pulNumLanguages,
     __out_ecount_opt(*pcchLanguagesBuffer) PZZWSTR pwszLanguagesBuffer,
@@ -360,12 +360,13 @@ extern "C" HRESULT DAPI LocAddString(
     )
 {
     HRESULT hr = S_OK;
+    LOC_STRING* pLocString = NULL;
 
     ++pWixLoc->cLocStrings;
     pWixLoc->rgLocStrings = static_cast<LOC_STRING*>(MemReAlloc(pWixLoc->rgLocStrings, sizeof(LOC_STRING) * pWixLoc->cLocStrings, TRUE));
     LocExitOnNull(pWixLoc->rgLocStrings, hr, E_OUTOFMEMORY, "Failed to reallocate memory for localization strings.");
 
-    LOC_STRING* pLocString = pWixLoc->rgLocStrings + (pWixLoc->cLocStrings - 1);
+    pLocString = pWixLoc->rgLocStrings + (pWixLoc->cLocStrings - 1);
 
     hr = StrAllocFormatted(&pLocString->wzId, L"#(loc.%s)", wzId);
     LocExitOnFailure(hr, "Failed to set localization string Id.");
diff --git a/src/libs/dutil/WixToolset.DUtil/logutil.cpp b/src/libs/dutil/WixToolset.DUtil/logutil.cpp
index 0a77a468..dae3c983 100644
--- a/src/libs/dutil/WixToolset.DUtil/logutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/logutil.cpp
@@ -825,7 +825,7 @@ static HRESULT LogStringWork(
 
         DWORD dwId = dwLogId & 0xFFFFFFF;
         DWORD dwType = dwLogId & 0xF0000000;
-        LPSTR szType = (0xE0000000 == dwType || REPORT_ERROR == rl) ? "e" : (0xA0000000 == dwType || REPORT_WARNING == rl) ? "w" : "i";
+        LPCSTR szType = (0xE0000000 == dwType || REPORT_ERROR == rl) ? "e" : (0xA0000000 == dwType || REPORT_WARNING == rl) ? "w" : "i";
 
         // add line prefix and trailing newline
         hr = StrAllocFormatted(&scz, L"%ls[%04X:%04X][%04hu-%02hu-%02huT%02hu:%02hu:%02hu]%hs%03d:%ls %ls%ls", LogUtil_sczSpecialBeginLine ? LogUtil_sczSpecialBeginLine : L"",
diff --git a/src/libs/dutil/WixToolset.DUtil/monutil.cpp b/src/libs/dutil/WixToolset.DUtil/monutil.cpp
index b42332ef..ea6a4b0a 100644
--- a/src/libs/dutil/WixToolset.DUtil/monutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/monutil.cpp
@@ -293,6 +293,7 @@ extern "C" HRESULT DAPI MonCreate(
 {
     HRESULT hr = S_OK;
     DWORD dwRetries = MON_THREAD_INIT_RETRIES;
+    MON_STRUCT *pm = NULL;
 
     MonExitOnNull(pHandle, hr, E_INVALIDARG, "Pointer to handle not specified while creating monitor");
 
@@ -300,7 +301,7 @@ extern "C" HRESULT DAPI MonCreate(
     *pHandle = static_cast<MON_HANDLE>(MemAlloc(sizeof(MON_STRUCT), TRUE));
     MonExitOnNull(*pHandle, hr, E_OUTOFMEMORY, "Failed to allocate monitor object");
 
-    MON_STRUCT *pm = static_cast<MON_STRUCT *>(*pHandle);
+    pm = static_cast<MON_STRUCT *>(*pHandle);
 
     pm->vpfMonGeneral = vpfMonGeneral;
     pm->vpfMonDriveStatus = vpfMonDriveStatus;
diff --git a/src/libs/dutil/WixToolset.DUtil/pathutil.cpp b/src/libs/dutil/WixToolset.DUtil/pathutil.cpp
index dd5385fc..af200b70 100644
--- a/src/libs/dutil/WixToolset.DUtil/pathutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/pathutil.cpp
@@ -464,6 +464,7 @@ DAPI_(HRESULT) PathFixedBackslashTerminate(
 {
     HRESULT hr = S_OK;
     size_t cchLength = 0;
+    LPWSTR wzLast = NULL;
 
     if (!cchPath)
     {
@@ -473,7 +474,7 @@ DAPI_(HRESULT) PathFixedBackslashTerminate(
     hr = ::StringCchLengthW(wzPath, cchPath, &cchLength);
     PathExitOnFailure(hr, "Failed to get length of path.");
 
-    LPWSTR wzLast = wzPath + (cchLength - 1);
+    wzLast = wzPath + (cchLength - 1);
     if (cchLength && L'/' == wzLast[0])
     {
         wzLast[0] = L'\\';
@@ -503,6 +504,7 @@ DAPI_(HRESULT) PathBackslashTerminate(
     HRESULT hr = S_OK;
     SIZE_T cchPath = 0;
     size_t cchLength = 0;
+    LPWSTR wzLast = NULL;
 
     hr = StrMaxLength(*psczPath, &cchPath);
     PathExitOnFailure(hr, "Failed to get size of path string.");
@@ -510,7 +512,7 @@ DAPI_(HRESULT) PathBackslashTerminate(
     hr = ::StringCchLengthW(*psczPath, cchPath, &cchLength);
     PathExitOnFailure(hr, "Failed to get length of path.");
 
-    LPWSTR wzLast = *psczPath + (cchLength - 1);
+    wzLast = *psczPath + (cchLength - 1);
     if (cchLength && L'/' == wzLast[0])
     {
         wzLast[0] = L'\\';
@@ -1364,6 +1366,8 @@ DAPI_(HRESULT) PathCompress(
 {
     HRESULT hr = S_OK;
     HANDLE hPath = INVALID_HANDLE_VALUE;
+    DWORD dwBytesReturned = 0;
+    USHORT usCompressionFormat;
 
     hPath = ::CreateFileW(wzPath, GENERIC_READ | GENERIC_WRITE, FILE_SHARE_READ | FILE_SHARE_DELETE, NULL, OPEN_EXISTING, FILE_FLAG_BACKUP_SEMANTICS, NULL);
     if (INVALID_HANDLE_VALUE == hPath)
@@ -1371,8 +1375,8 @@ DAPI_(HRESULT) PathCompress(
         PathExitWithLastError(hr, "Failed to open path %ls for compression.", wzPath);
     }
 
-    DWORD dwBytesReturned = 0;
-    USHORT usCompressionFormat = COMPRESSION_FORMAT_DEFAULT;
+    dwBytesReturned = 0;
+    usCompressionFormat = COMPRESSION_FORMAT_DEFAULT;
     if (0 == ::DeviceIoControl(hPath, FSCTL_SET_COMPRESSION, &usCompressionFormat, sizeof(usCompressionFormat), NULL, 0, &dwBytesReturned, NULL))
     {
         // ignore compression attempts on file systems that don't support it
diff --git a/src/libs/dutil/WixToolset.DUtil/pipeutil.cpp b/src/libs/dutil/WixToolset.DUtil/pipeutil.cpp
index 8d0a5ed0..83b769d3 100644
--- a/src/libs/dutil/WixToolset.DUtil/pipeutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/pipeutil.cpp
@@ -457,6 +457,7 @@ DAPI_(HRESULT) PipeServerWaitForClientConnect(
 {
     HRESULT hr = S_OK;
     DWORD dwPipeState = PIPE_READMODE_BYTE | PIPE_NOWAIT;
+    DWORD cRetry = 0;
 
     // Temporarily make the pipe non-blocking so we will not get stuck in ::ConnectNamedPipe() forever
     // if the client decides not to show up.
@@ -466,7 +467,6 @@ DAPI_(HRESULT) PipeServerWaitForClientConnect(
     }
 
     // Loop for a while waiting for a connection from client process.
-    DWORD cRetry = 0;
     do
     {
         if (!::ConnectNamedPipe(hPipe, NULL))
diff --git a/src/libs/dutil/WixToolset.DUtil/precomp.h b/src/libs/dutil/WixToolset.DUtil/precomp.h
index 81dc7992..2c3bbcda 100644
--- a/src/libs/dutil/WixToolset.DUtil/precomp.h
+++ b/src/libs/dutil/WixToolset.DUtil/precomp.h
@@ -3,16 +3,17 @@
 
 
 #ifndef _WIN32_WINNT
-#define _WIN32_WINNT 0x0500
+#define _WIN32_WINNT 0x0600
 #endif
 
 #ifndef _WIN32_MSI
-#define _WIN32_MSI 200
+#define _WIN32_MSI 500
 #endif
 
-#define JET_VERSION 0x0501
+#define JET_VERSION 0x0600  // work around a bug in MinGW esent.h
 
-#include <WinSock2.h>
+#include <sal.h>
+#include <winsock2.h>
 #include <windows.h>
 #include <windowsx.h>
 #include <intsafe.h>
@@ -24,22 +25,41 @@
 #include <shlobj.h>
 #include <shlwapi.h>
 #include <gdiplus.h>
-#include <Tlhelp32.h>
+#include <tlhelp32.h>
 #include <lm.h>
-#include <Iads.h>
+#include <iads.h>
 #include <activeds.h>
 #include <richedit.h>
 #include <stddef.h>
+
 #include <esent.h>
-#include <ahadmin.h>
-#include <SRRestorePtAPI.h>
+#define JET_bitColumnFixed 0x1
+#define JET_bitColumnTagged 0x2
+#define JET_bitColumnAutoincrement 0x10
+#define JET_bitColumnNotNULL 0x4
+#define JET_bitIndexUnique 0x1
+#define JET_bitIndexPrimary 0x2
+#define JET_bitNewKey 0x1
+#define JET_bitFullColumnStartLimit 0x100
+#define JET_bitFullColumnEndLimit 0x200
+#define JET_bitSeekEQ 0x1
+#define JET_bitSeekLE 0x4
+#define JET_bitSeekGE 0x8
+#define JET_bitSeekGT 0x10
+#define JET_bitRangeUpperLimit 0x2
+#define JET_bitRangeRemove 0x8
+#define JET_bitRollbackAll 0x1
+#define JET_prepCancel 3
+#define JetCreateTableColumnIndex JetCreateTableColumnIndexA
+
+#include <srrestoreptapi.h>
 #include <userenv.h>
-#include <WinIoCtl.h>
+#include <winioctl.h>
 #include <wtsapi32.h>
 #include <wuapi.h>
 #include <commctrl.h>
 #include <dbt.h>
-#include <ShellScalingApi.h>
+#include <shellscalingapi.h>
 
 #include "dutilsources.h"
 #include "dutil.h"
@@ -102,3 +122,14 @@
 #include <msxml2.h>   // This file is needed to include xmlutil.h
 #include "xmlutil.h"
 
+#define SHORT_MIN -32768
+#define SHORT_MAX 32767
+#define USHORT_MAX 65535
+#define WORD_MAX 65535
+#define DWORD_MAX __MSABI_LONG(0xffffffffu)
+#define STRSAFE_MAX_LENGTH  (STRSAFE_MAX_CCH - 1)
+
+template<class A, class B>
+constexpr std::common_type_t<A, B> min(A a, B b) { return a < b ? a : b; }
+template<class A, class B>
+constexpr std::common_type_t<A, B> max(A a, B b) { return a > b ? a : b; }
diff --git a/src/libs/dutil/WixToolset.DUtil/regutil.cpp b/src/libs/dutil/WixToolset.DUtil/regutil.cpp
index 744e7a3f..f2586c97 100644
--- a/src/libs/dutil/WixToolset.DUtil/regutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/regutil.cpp
@@ -953,7 +953,7 @@ DAPI_(HRESULT) RegWriteNone(
     HRESULT hr = S_OK;
     DWORD er = ERROR_SUCCESS;
 
-    er = vpfnRegSetValueExW(hk, wzName, 0, REG_NONE, NULL, NULL);
+    er = vpfnRegSetValueExW(hk, wzName, 0, REG_NONE, NULL, 0);
     RegExitOnWin32Error(er, hr, "Failed to set %ls value.", wzName);
 
 LExit:
diff --git a/src/libs/dutil/WixToolset.DUtil/rexutil.cpp b/src/libs/dutil/WixToolset.DUtil/rexutil.cpp
index ce28beb3..897c41c1 100644
--- a/src/libs/dutil/WixToolset.DUtil/rexutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/rexutil.cpp
@@ -135,6 +135,8 @@ extern "C" HRESULT RexExtract(
 
     REX_CALLBACK_STRUCT rcs = { };
 
+    char empty[] = "";
+
     // remember the write callback
     vpfnWrite = pfnWrite;
 
@@ -175,7 +177,7 @@ extern "C" HRESULT RexExtract(
     rcs.pfnProgress = pfnProgress;
     rcs.pvContext = pvContext;
 
-    fResult = ::FDICopy(vhfdi, vpszResource, "", 0, RexCallback, NULL, static_cast<void*>(&rcs));
+    fResult = ::FDICopy(vhfdi, vpszResource, empty, 0, RexCallback, NULL, static_cast<void*>(&rcs));
     if (!fResult && !rcs.fStopExtracting)   // if something went wrong and it wasn't us just stopping the extraction, then return a failure
     {
         hr = vhrLastError;  // TODO: put verf info in trace message here
diff --git a/src/libs/dutil/WixToolset.DUtil/rssutil.cpp b/src/libs/dutil/WixToolset.DUtil/rssutil.cpp
index 8f994dfc..29320123 100644
--- a/src/libs/dutil/WixToolset.DUtil/rssutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/rssutil.cpp
@@ -473,6 +473,7 @@ static HRESULT ParseRssUnknownElement(
     IXMLDOMNamedNodeMap* pixnnmAttributes = NULL;
     IXMLDOMNode* pixnAttribute = NULL;
     RSS_UNKNOWN_ELEMENT* pNewUnknownElement;
+    RSS_UNKNOWN_ELEMENT** ppTail = NULL;
 
     pNewUnknownElement = static_cast<RSS_UNKNOWN_ELEMENT*>(MemAlloc(sizeof(RSS_UNKNOWN_ELEMENT), TRUE));
     RssExitOnNull(pNewUnknownElement, hr, E_OUTOFMEMORY, "Failed to allocate unknown element.");
@@ -518,7 +519,7 @@ static HRESULT ParseRssUnknownElement(
     }
     RssExitOnFailure(hr, "Failed to enumerate all attributes on RSS unknown element.");
 
-    RSS_UNKNOWN_ELEMENT** ppTail = ppUnknownElement;
+    ppTail = ppUnknownElement;
     while (*ppTail)
     {
         ppTail = &(*ppTail)->pNext;
@@ -556,6 +557,7 @@ static HRESULT ParseRssUnknownAttribute(
     BSTR bstrNodeName = NULL;
     BSTR bstrNodeValue = NULL;
     RSS_UNKNOWN_ATTRIBUTE* pNewUnknownAttribute;
+    RSS_UNKNOWN_ATTRIBUTE** ppTail = NULL;
 
     pNewUnknownAttribute = static_cast<RSS_UNKNOWN_ATTRIBUTE*>(MemAlloc(sizeof(RSS_UNKNOWN_ATTRIBUTE), TRUE));
     RssExitOnNull(pNewUnknownAttribute, hr, E_OUTOFMEMORY, "Failed to allocate unknown attribute.");
@@ -584,7 +586,7 @@ static HRESULT ParseRssUnknownAttribute(
     hr = StrAllocString(&pNewUnknownAttribute->wzValue, bstrNodeValue, 0);
     RssExitOnFailure(hr, "Failed to allocate RSS unknown attribute value.");
 
-    RSS_UNKNOWN_ATTRIBUTE** ppTail = ppUnknownAttribute;
+    ppTail = ppUnknownAttribute;
     while (*ppTail)
     {
         ppTail = &(*ppTail)->pNext;
diff --git a/src/libs/dutil/WixToolset.DUtil/sceutil.cpp b/src/libs/dutil/WixToolset.DUtil/sceutil.cpp
index 590c937a..1fc1d7e3 100644
--- a/src/libs/dutil/WixToolset.DUtil/sceutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/sceutil.cpp
@@ -2,6 +2,8 @@
 
 #include "precomp.h"
 
+#if 0
+
 #include "sceutil.h"
 
 // Limit is documented as 4 GB, but for some reason the API's don't let us specify anything above 4091 MB.
@@ -2485,3 +2487,5 @@ static void ReleaseDatabaseInternal(
 }
 
 #endif // end SKIP_SCE_COMPILE
+
+#endif
diff --git a/src/libs/dutil/WixToolset.DUtil/sqlutil.cpp b/src/libs/dutil/WixToolset.DUtil/sqlutil.cpp
index 782c7088..223c3e39 100644
--- a/src/libs/dutil/WixToolset.DUtil/sqlutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/sqlutil.cpp
@@ -2,6 +2,8 @@
 
 #include "precomp.h"
 
+#if 0
+
 // okay, this may look a little weird, but sqlutil.h cannot be in the 
 // pre-compiled header because we need to #define these things so the
 // correct GUID's get pulled into this object file
@@ -1000,3 +1002,5 @@ LExit:
     ReleaseStr(pwz);
     return hr;
 }
+
+#endif
diff --git a/src/libs/dutil/WixToolset.DUtil/thmutil.cpp b/src/libs/dutil/WixToolset.DUtil/thmutil.cpp
index 36b187a7..9ec58189 100644
--- a/src/libs/dutil/WixToolset.DUtil/thmutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/thmutil.cpp
@@ -5575,6 +5575,8 @@ static HRESULT ShowControl(
     BOOL fHide = SW_HIDE == nCmdShow;
     THEME* pTheme = pControl->pTheme;
     THEME_PAGE* pPage = ThemeGetPage(pTheme, dwPageId);
+    HWND hWnd = 0;
+    BOOL fEnabled = false, fVisible = false;
 
     // Save the editbox value if necessary (other control types save their values immediately).
     if (pTheme->pfnSetStringVariable && pControl->fAutomaticValue &&
@@ -5587,7 +5589,7 @@ static HRESULT ShowControl(
         ThmExitOnFailure(hr, "Failed to set the variable '%ls' to '%ls'", pControl->sczName, sczText);
     }
 
-    HWND hWnd = pControl->hWnd;
+    hWnd = pControl->hWnd;
 
     if (fHide && pControl->wPageId)
     {
@@ -5601,8 +5603,8 @@ static HRESULT ShowControl(
         ExitFunction();
     }
 
-    BOOL fEnabled = !(pControl->dwInternalStyle & INTERNAL_CONTROL_STYLE_DISABLED);
-    BOOL fVisible = !(pControl->dwInternalStyle & INTERNAL_CONTROL_STYLE_HIDDEN);
+    fEnabled = !(pControl->dwInternalStyle & INTERNAL_CONTROL_STYLE_DISABLED);
+    fVisible = !(pControl->dwInternalStyle & INTERNAL_CONTROL_STYLE_HIDDEN);
 
     if (pTheme->pfnEvaluateCondition)
     {
diff --git a/src/libs/dutil/WixToolset.DUtil/wiutil.cpp b/src/libs/dutil/WixToolset.DUtil/wiutil.cpp
index dac57a3d..fc62ad82 100644
--- a/src/libs/dutil/WixToolset.DUtil/wiutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/wiutil.cpp
@@ -1064,11 +1064,11 @@ static INT CALLBACK InstallEngineCallback(
     {
         if (INSTALLMESSAGE_PROGRESS == mt)
         {
-            nResult = HandleInstallProgress(pContext, wzMessage, NULL);
+            nResult = HandleInstallProgress(pContext, wzMessage, 0);
         }
         else
         {
-            nResult = HandleInstallMessage(pContext, mt, uiFlags, wzMessage, NULL);
+            nResult = HandleInstallMessage(pContext, mt, uiFlags, wzMessage, 0);
         }
     }
 
@@ -1089,6 +1089,7 @@ static INT CALLBACK InstallEngineRecordCallback(
     UINT uiFlags = 0x00FFFFFF & uiMessage;
     LPWSTR sczMessage = NULL;
     DWORD cchMessage = 0;
+    wchar_t empty[] = L"";
 
     if (hRecord)
     {
@@ -1101,7 +1102,7 @@ static INT CALLBACK InstallEngineRecordCallback(
             // create formated message string
 #pragma prefast(push)
 #pragma prefast(disable:6298) // docs explicitly say this is a valid option for getting the buffer size
-            DWORD er = ::MsiFormatRecordW(NULL, hRecord, L"", &cchMessage);
+            DWORD er = ::MsiFormatRecordW(0, hRecord, empty, &cchMessage);
 #pragma prefast(pop)
             if (ERROR_MORE_DATA == er || ERROR_SUCCESS == er)
             {
@@ -1113,7 +1114,7 @@ static INT CALLBACK InstallEngineRecordCallback(
             }
             WiuExitOnFailure(hr, "Failed to allocate string for formated message.");
 
-            er = ::MsiFormatRecordW(NULL, hRecord, sczMessage, &cchMessage);
+            er = ::MsiFormatRecordW(0, hRecord, sczMessage, &cchMessage);
             WiuExitOnWin32Error(er, hr, "Failed to format message record.");
 
             // Pass to handler including both the formated message and the original record.
@@ -1602,6 +1603,7 @@ void InitializeMessageData(
 {
     DWORD cData = 0;
     LPWSTR* rgsczData = NULL;
+    wchar_t empty[] = L"";
 
     // If we have a record based message, try to get the extra data.
     if (hRecord)
@@ -1619,7 +1621,7 @@ void InitializeMessageData(
             // get string from record
 #pragma prefast(push)
 #pragma prefast(disable:6298)
-            DWORD er = ::MsiRecordGetStringW(hRecord, i + 1, L"", &cch);
+            DWORD er = ::MsiRecordGetStringW(hRecord, i + 1, empty, &cch);
 #pragma prefast(pop)
             if (ERROR_MORE_DATA == er)
             {
diff --git a/src/libs/dutil/WixToolset.DUtil/xmlutil.cpp b/src/libs/dutil/WixToolset.DUtil/xmlutil.cpp
index 2e1a2200..82c01b47 100644
--- a/src/libs/dutil/WixToolset.DUtil/xmlutil.cpp
+++ b/src/libs/dutil/WixToolset.DUtil/xmlutil.cpp
@@ -32,6 +32,7 @@ extern "C" HRESULT DAPI XmlInitialize(
     )
 {
     HRESULT hr = S_OK;
+    LONG cInitialized = false;
 
     if (!fComInitialized)
     {
@@ -43,7 +44,7 @@ extern "C" HRESULT DAPI XmlInitialize(
         }
     }
 
-    LONG cInitialized = ::InterlockedIncrement(&vcXmlInitialized);
+    cInitialized = ::InterlockedIncrement(&vcXmlInitialized);
     if (1 == cInitialized)
     {
         // NOTE: 4.0 behaves differently than 3.0 so there may be problems doing this
diff --git a/src/wix/wixnative/certhashes.cpp b/src/wix/wixnative/certhashes.cpp
index cbb548ba..05a1deb1 100644
--- a/src/wix/wixnative/certhashes.cpp
+++ b/src/wix/wixnative/certhashes.cpp
@@ -80,7 +80,7 @@ static HRESULT GetPublicKeyIdentifierAndThumbprint(
     pCertContext = pChainContext->rgpChain[0]->rgpElement[0]->pCertContext;
 
     // Get the certificate's public key identifier and thumbprint.
-    if (!::CryptHashPublicKeyInfo(NULL, CALG_SHA1, 0, X509_ASN_ENCODING, &pCertContext->pCertInfo->SubjectPublicKeyInfo, rgbPublicKeyIdentifier, &cbPublicKeyIdentifier))
+    if (!::CryptHashPublicKeyInfo(0, CALG_SHA1, 0, X509_ASN_ENCODING, &pCertContext->pCertInfo->SubjectPublicKeyInfo, rgbPublicKeyIdentifier, &cbPublicKeyIdentifier))
     {
         ExitWithLastError(hr, "Failed to get certificate public key identifier from file: %ls", wzPath);
     }
